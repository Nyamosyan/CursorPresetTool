<Window x:Class="CursorPresetTool.Gui.NewPresetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新しいプリセットの作成"
        Height="220"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="新しいプリセットの作成方法を選択してください。"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8" />

        <StackPanel Grid.Row="1"
                    Margin="8,0,0,0">
            <RadioButton x:Name="EmptyRadio"
                         Content="空のプリセットから作成"
                         IsChecked="True"
                         Margin="0,0,0,4" />
            <RadioButton x:Name="FromSystemRadio"
                         Content="現在のカーソル設定から作成"
                         Margin="0,0,0,4" />
            <RadioButton x:Name="FromSelectedRadio"
                         Content="選択中のプリセットをコピーして作成"
                         Margin="0,0,0,4" />
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="OK"
                    Width="80"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="OkButton_Click" />
            <Button Content="キャンセル"
                    Width="80"
                    IsCancel="True"
                    Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
